import { AssetPipePipe } from './asset-pipe.pipe';

describe('AssetPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetPipePipe();
    expect(pipe).toBeTruthy();
  });
});
